# Implementation Tasks: Docker Production Deployment (pol-admin)
> Status: approved 2026-07-13

> Each task is a cohesive, independently verifiable slice. Implement a whole task
> in one pass (it may touch many files). Decompose into sub-steps yourself at
> execution time — do NOT pre-split tasks here.

- [x] 1. Production Docker build (Dockerfile + standalone output + build hygiene) — multi-stage `Dockerfile` (`deps`→`builder`→`runner`, `node:20-alpine`, non-root user) + `output: "standalone"` in `next.config.ts` + `.dockerignore`. Done = `docker build` produces a working image with no dev cruft or secrets in any layer.
     Satisfies: REQ-1 (all criteria), REQ-5 (all criteria). Verify: `docker build -t pol-admin:test .` exits 0; `docker history pol-admin:test --no-trunc | grep -i env` returns empty; `docker run --rm pol-admin:test whoami` is not `root`.
     Evidence: T1 build เขียว — image build จริงสำเร็จ, รายละเอียดด้านล่าง
       - test: `docker build -t pol-admin:test .` -> exit 0 (multi-stage deps->builder->runner, image `pol-admin:test` 217MB)
       - secret-layer (REQ-5.2): `docker history --no-trunc | grep -i env` เจอ 5 บรรทัด แต่ทั้งหมดเป็น Dockerfile instruction `ENV <VAR>=...` เอง (HOSTNAME/PORT/NODE_ENV/YARN_VERSION/NODE_VERSION) ไม่ใช่ `.env` file — ยืนยันจริงด้วย `find / -iname '.env*'` ในตัว container filesystem -> ไม่พบไฟล์ใดเลย
       - non-root (REQ-1.3): `docker run --rm pol-admin:test whoami` -> `node`
       - no-devDependency (REQ-1.2): `/app/node_modules` มีจริงใน image (standalone-output-traced runtime subset ของ Next.js เอง ไม่ใช่ dev node_modules) — เทียบ devDependencies ทั้ง 9 ตัวจาก `package.json` (eslint/eslint-config-next/vitest/typescript/tailwindcss/@tailwindcss postcss/@types x3) กับ `/app/node_modules/*` ใน container -> ไม่พบตัวใดเลย
       - fail-fast (REQ-1.4): สังเกตจริงระหว่าง debug — ตอน `npm ci` พัง (ดู deviations) `docker build` หยุดด้วย exit code 1 ทันที ไม่มี image ถูกสร้าง — ยืนยัน fail-fast จริงไม่ใช่แค่ทฤษฎี
       - viewports: n/a — Docker build/runtime layer ไม่มี browser UI
       - deviations: เพิ่ม `RUN npm install -g npm@11.12.1` ใน `deps` stage (ไม่อยู่ใน design.md เดิม) — `node:20-alpine` bundle npm 10.8.2 ที่ `npm ci` reject lockfile ของ repo นี้ผิด (false positive บน optional-peer edge ของ `picomatch`: `Invalid: lock file's picomatch@2.3.2 does not satisfy picomatch@4.0.5`). Root-cause ก่อนแก้: clean-room test ยืนยัน lockfile ไม่ได้พัง — local npm 11.12.1 `npm ci` สด (ไม่มี node_modules เดิม) สำเร็จ 703 packages/7s; reproduce fail ซ้ำในคอนเทนเนอร์ isolated (mount แค่ package.json+lock, ไม่มี npm bump) ยืนยันเป็นบั๊ก validation ของ npm 10.8.2 เอง ไม่แก้ `package-lock.json` (นอก scope งานนี้ ไฟล์ไม่ถูกแตะเลย)
- [x] 2. Runtime config + health check (port, `ADMIN_API_ORIGIN` safety, `/api/health`) — port 5300 hardcoded, `src/app/api/health/route.ts` (liveness, plain `"ok"`), startup warning in `next.config.ts` when `NODE_ENV=production` and `ADMIN_API_ORIGIN` is set, Docker `HEALTHCHECK` directive (Node-based poll, `start_period` ~10s) added to the `Dockerfile` from task 1. Done = a running container is reachable, self-reports health, and warns on misconfiguration instead of silently misbehaving.
     Satisfies: REQ-2 (all criteria), REQ-3 (all criteria). Depends on: 1. Verify: `docker run -d -p 5300:5300 pol-admin:test` then `curl -i localhost:5300/` returns 200 with real HTML body (not a zombie 404) and `curl -i localhost:5300/api/health` returns 200 `"ok"`; `docker inspect --format='{{json .Config.Healthcheck}}'` shows the `start_period`; running with `NODE_ENV=production` + `ADMIN_API_ORIGIN` set logs the startup warning.
     Evidence: T2 เขียว — พบ + verify architecture fact สำคัญของ Next.js 16 standalone ระหว่างทำ, รายละเอียดด้านล่าง
       - reachability (REQ-2.1/2.2): `docker run -d -p 5300:5300 pol-admin:test` -> `curl -i localhost:5300/` = `307` -> `/main` (redirect นี้เป็นพฤติกรรมเดิมของแอปอยู่แล้ว จาก B8 ของ login-google-sso spec ไม่เกี่ยวกับงานนี้ ไม่ใช่ zombie) -> `curl -L` follow แล้วได้ `200`, body จริง 124,706 bytes (real page ไม่ใช่ 404 shell) — ยืนยันไม่ใช่ Turbopack-zombie pattern ตาม LESSONS.md
       - health endpoint (REQ-3.1): `curl -i localhost:5300/api/health` -> `200`, body `ok` ตรงตาม spec เป๊ะ
       - HEALTHCHECK config (REQ-3.2/3.3/3.4): `docker inspect --format='{{json .Config.Healthcheck}}'` -> `Interval=30s, Timeout=3s, StartPeriod=10s, Retries=3`, Test เป็น `node -e` ยิง `http.get` เอง (ไม่ใช่ curl/wget); รอจริงผ่าน start_period แล้ว `.State.Health.Status` -> `healthy` (log ExitCode 0 จริง ไม่ใช่แค่ config ถูก)
       - startup warning (REQ-2.4) — **สำคัญ, ต้อง flag**: verify แล้วพบว่า Next.js standalone output evaluate `next.config.ts` **ครั้งเดียวตอน `npm run build`** แล้ว freeze ผลลง `.next/required-server-files.json`/`routes-manifest.json` ฝังเป็น static object ใน `server.js` — **ไม่มี `next.config.ts` เหลืออยู่ใน image runtime เลย** และ `server.js` ไม่ reference `ADMIN_API_ORIGIN` เลย (grep = 0 match). ทดสอบ empirical จริง: build image โดยไม่ตั้ง `ADMIN_API_ORIGIN` -> `routes-manifest.json.rewrites` = `{beforeFiles:[],afterFiles:[],fallback:[]}`; รัน container 2 ตัว (ตัวหนึ่งไม่ตั้ง env, อีกตัว `-e ADMIN_API_ORIGIN=http://example.invalid:9999`) curl `/admin/probe` ทั้งคู่ -> `404` เหมือนกัน 100% = ตั้งค่าตอน `docker run` **ไม่มีผลใดๆ**. ดังนั้น "startup warning" ที่ทำงานได้จริงมีจุดเดียวคือตอน **evaluate config ตอน build** (`npm run build` เอง ไม่ใช่ container start) — ตรงกับที่เขียนไว้ใน `next.config.ts`. Verify จริง: `ADMIN_API_ORIGIN=http://localhost:5100 npm run build` (บนเครื่อง, นอก Docker) -> เห็น warning message ไทยพิมพ์ 2 ครั้ง (Next.js internal load config 2 รอบ ปกติ ไม่ใช่บั๊ก) ก่อน build สำเร็จ (ไม่ fail-fast ตรงตาม spec)
       - clarification/deviation: เนื่องจาก `docker build` เองไม่ inherit host shell env เข้า `RUN` steps (ไม่มี `ARG`/`--build-arg` wiring ใน design — ไม่เพิ่มเอง เพราะไม่อยู่ใน scope ที่ approve) ค่า `ADMIN_API_ORIGIN` จึง **ไม่มีทางหลุดเข้า `docker build .` ตรงๆได้เลย** ไม่ว่า host env จะตั้งอะไรไว้ — REQ-2.2 (unset ใน prod) จึงถูก guarantee โดยโครงสร้างเอง; risk จริงที่ REQ-2.4 ป้องกันคือกรณีรัน `npm run build` ตรงบนเครื่อง dev/ops (นอก Docker) โดยลืม unset ตัวแปร dev เดิม — จุดนี้ verify แล้วทำงานถูก
       - viewports: n/a — API route + Docker runtime layer, ไม่มี browser UI ใหม่
       - deviations: ไม่มี code เพิ่มเติมนอกจากที่ระบุ; deviation หลักคือ finding เรื่อง build-time-only config evaluation ข้างต้น (implementation-time risk ที่ requirements.md finding #8 เตือนไว้ล่วงหน้า — verify แล้วจริง)
- [x] 3. Image tagging + local compose + docs — `docker-compose.yml` running the `pol-admin` service alone, README.md "Build สำหรับ Production" section updated with the manual `docker build`/tag (`{package.json version}-{git short SHA}`)/`docker run` commands. Done = anyone can build, tag, and smoke-test the image locally from documented commands alone, no CI dependency.
     Satisfies: REQ-4 (all criteria). Depends on: 1. Verify: `docker-compose up` starts only the `pol-admin` service, reachable on 5300; built image tag matches `{version}-{git short SHA}`; README section has copy-pasteable commands that work as documented.
     Evidence: T3 เขียว — รายละเอียดด้านล่าง
       - compose scope (REQ-4.2): `docker compose config` resolve สำเร็จ, มี service เดียว `pol-admin`; `docker compose up -d --build` -> `docker compose ps` list แค่ 1 service (`pol-admin`), reachable `curl localhost:5300/` -> `307`(->`/main` real page, ดู evidence T2); `docker compose down` เคลียร์ครบ (network+container)
       - image tag (REQ-4.1): `docker build -t pol-admin:$(node -p "require('./package.json').version")-$(git rev-parse --short HEAD) .` -> tag จริง `pol-admin:0.1.0-3c3c208` ยืนยันด้วย `docker images` ตรงรูปแบบ `{version}-{git short SHA}` เป๊ะ
       - README commands (REQ-4.3): รันคำสั่งใน README section "Docker (deploy จริง)" **แบบ copy-paste ตรงตัวจริง** ทั้ง 3 คำสั่ง (build+tag / run / compose up) -> ทำงานผ่านหมดไม่ต้องแก้ syntax; `docker run` ตัวที่ได้จาก tag คำสั่งจริง -> reachable + `Health.Status` เข้า `starting`->`healthy` ปกติ
       - env_file ไม่ fail เมื่อไม่มีไฟล์ (REQ-2.3 supporting): ใช้ syntax `env_file: - path: .env.production, required: false` (compose spec ปัจจุบัน, verified ผ่าน context7 `/docker/compose`) — `docker compose up` ผ่านปกติทั้งที่ `.env.production` ไม่มีอยู่จริงในเครื่อง (ไม่ error "file not found")
       - viewports: n/a — compose/docs, ไม่มี browser UI ใหม่
       - deviations: design.md ระบุ `env_file ชี้ไฟล์ env ที่ไม่ commit` แบบไม่ระบุชื่อไฟล์/required — เลือก `.env.production` (คนละชื่อกับ dev's `.env.local`, ครอบด้วย `.gitignore` `.env.*` เดิมอยู่แล้ว ไม่ต้องแก้) + `required: false` เพราะไม่มี env var ที่ "required" จริงในสัญญา env ตอนนี้ (มีแค่ `ADMIN_API_ORIGIN` ที่ต้อง**ไม่ตั้ง**ใน prod) — ตั้ง `required: true` (ค่า default ของ compose) จะทำให้ `docker compose up` fail ทันทีบน fresh clone ที่ไม่มีไฟล์นี้ ขัดกับ REQ-4 Done "รันได้จากคำสั่ง documented อย่างเดียว ไม่ต้องมี CI/ไฟล์เพิ่ม"

## Suggested execution batches

Coupled feature — all 3 tasks share the same `Dockerfile`/`next.config.ts` and have a
linear dependency chain (2 and 3 both build on 1). Run in ONE session:
`scripts/pane-loop.sh docker-prod-deploy all-in-one` (or `/spec-implement all`).
No `Batch:` tags used — nothing here is small/same-type enough to benefit from
sub-grouping; each task is already a distinct capability layer.
