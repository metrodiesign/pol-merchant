# Handoff Note: Bugfix Audit Hardening

> From: Claude Code session `941ba8b8-68f9-405f-babd-1eaa738a7d1e`  To: session ถัดไป  Date: 2026-08-28

## Task Summary

งานคือแก้ confirmed runtime bugs และปิด evidence/contract gaps จาก bug report ทั้งชุดใน `pol-admin`
โดยแยกเป็น 5 sub-project: smoke cleanup, PSP RBAC, Admin proxy TLS, self-hosted fonts และ Admin auth.
Root cause ผ่านการตรวจ read-only และผู้ใช้ยืนยันแล้ว โดยใช้ decisions: `permissions: []` คง Inline 403,
smoke เป็น Admin-only และ self-host fonts.

## Current Status

งาน implementation ตาม approved bugfix specs ทั้ง 5 ชุดทำครบและทุก task ใน tasks.md ถูก mark `[x]`:

- `bugfix-smoke-cleanup-hardening`: แก้ IPv4/IPv6 preflight, detached descendant cleanup และ Admin-only smoke
- `bugfix-psp-rbac-contract`: ล็อก Inline 403 สำหรับ `permissions: []`, route/API PSP denial ไป `/error/403`
- `bugfix-admin-proxy-evidence`: เพิ่ม Next proxy integration test, local-origin CA guard และ browser evidence
- `bugfix-font-self-hosting`: เปลี่ยน `next/font/google` เป็น `next/font/local` พร้อม local assets และ license
- `bugfix-auth-evidence-and-docs`: เติม logout fixture/evidence และแก้เอกสาร Microsoft SSO

สถานะยังไม่ควรปิดงานระดับระบบทั้งหมด เพราะยังมีข้อจำกัดใน `Known Issues` โดยเฉพาะ Windows process
cleanup และ authenticated production evidence. ยังไม่มี commit หรือ push.

## Files Changed

ไฟล์ที่แก้หรือสร้างโดยงานนี้:

- `.claude/specs/bugfix-smoke-cleanup-hardening/bugfix.md` — approved bugfix scope ปัจจุบัน
- `.claude/specs/bugfix-smoke-cleanup-hardening/tasks.md` — task evidence ครบ 3 งาน
- `.claude/specs/bugfix-psp-rbac-contract/bugfix.md` — PSP account/route/API contract
- `.claude/specs/bugfix-psp-rbac-contract/tasks.md` — task evidence ครบ 3 งาน
- `.claude/specs/bugfix-admin-proxy-evidence/bugfix.md` — proxy evidence scope
- `.claude/specs/bugfix-admin-proxy-evidence/tasks.md` — task evidence ครบ 3 งาน
- `.claude/specs/bugfix-font-self-hosting/bugfix.md` — local font contract
- `.claude/specs/bugfix-font-self-hosting/tasks.md` — task evidence ครบ 3 งาน
- `.claude/specs/bugfix-auth-evidence-and-docs/bugfix.md` — auth evidence scope
- `.claude/specs/bugfix-auth-evidence-and-docs/tasks.md` — task evidence ครบ 3 งาน
- `.claude/specs/bugfix-psp-rbac-403/bugfix.md` — เพิ่ม scope reconciliation เรื่อง `permissions: []`
- `.claude/specs/bugfix-smoke-route-cleanup/bugfix.md` — เพิ่ม scope reconciliation เป็น Admin-only
- `.claude/specs/bugfix-smoke-route-cleanup/tasks.md` — ระบุ Merchant/`3002` เป็น historical evidence
- `.claude/specs/bugfix-admin-logout-sso/bugfix.md` — เพิ่ม evidence reconciliation
- `.claude/specs/bugfix-admin-logout-sso/tasks.md` — refresh test/evidence/version และข้อจำกัด
- `.claude/specs/login-google-sso/design.md` — แก้ logout branch ไม่ใช้ `.finally`
- `docs/dev-setup.md` — แก้ Admin login เป็น Microsoft และระบุ OIDC parameters
- `scripts/lib/workspace-verification.mjs` — ตรวจ port `127.0.0.1`, `::1`, `::`
- `scripts/lib/workspace-process.mjs` — track process groups และ cleanup detached descendants
- `scripts/smoke-workspace-routes.mjs` — เริ่ม process-tree tracking หลัง spawn
- `scripts/dev-tls-ca.cjs` — เพิ่ม local HTTPS origin guard; คง CommonJS preload
- `scripts/psp-contract-server.mjs` — เพิ่ม scenarios `list-forbidden`, `logout-success`, logout failure
- `scripts/lib/workspace-verification.test.mjs` — IPv6/wildcard, detached cleanup, proxy, CA, offline build และ font tests
- `src/app/layout.tsx` — เปลี่ยน Google font loader เป็น local font loader
- `src/app/fonts/` — local TrueType assets 11 ไฟล์ และ `LICENSE-*.txt` 7 ไฟล์
- `src/components/auth/auth-guard.test.ts` — rendered Inline 403 regression test
- `.claude/handoff-2026-08-28.md` — handoff ฉบับนี้

ไฟล์ dirty/untracked ที่มีอยู่ก่อนหรือเป็นงานอื่น ห้าม revert/แก้รวมกับงานนี้โดยไม่ตรวจ scope:

- `src/app/control/psp/list/page.tsx`
- `src/components/control/psp/connections-view.tsx`
- `src/components/control/shared/list-toolbar.tsx`
- `src/lib/api/admin/division.ts`, `division.test.ts`
- `src/lib/api/admin/level.ts`, `level.test.ts`
- `src/lib/api/admin/office.ts`, `office.test.ts`
- `src/lib/api/admin/position.ts`, `position.test.ts`
- `.claude/specs/pol-core-api-integration/`
- `src/lib/api/admin/org-read-contract.ts`, `org-read-contract.test.ts`
- `src/app/control/psp/list/page.test.ts`
- `psp-browser-network.txt`, `psp-list-1440.png`, `psp-list-1440-fixed.png`, `real-backend-psp-network.txt`

## Important Decisions

- `permissions: []` เป็น account-level authenticated no-access จึงคง Inline 403 เพื่อไม่สร้าง redirect loop.
- route-level missing `settings.manage` และ PSP list API `403` ใช้ `/error/403`.
- smoke current architecture เป็น Admin-only, ตรวจ port `3001`; ห้ามนำ Merchant/`3002` กลับมา.
- local TLS CA เพิ่มเฉพาะ local HTTPS origin ที่ตรวจได้; ห้ามปิด TLS verification.
- font assets ใช้ upstream official Google Fonts repository และ SIL OFL 1.1; ใช้ TrueType ไม่ใช่ `.woff2`.
- proxy integration ใช้ temporary self-signed certificate ที่สร้างด้วย `openssl`; test ไม่เปิดเผย key และลบ temp directory.
- process cleanup ใช้ `ps` บน Unix และบันทึก process-group IDs ก่อน leader ปิด; มีการ capture ซ้ำก่อน signal.
- ผู้ใช้อนุญาตให้ล้างเฉพาะ generated `.next/cache/turbopack` เพื่อพิสูจน์ clean-cache build.
- PID `11610` ที่ครอง port `3001` ถูกผู้ใช้หยุดเอง; ห้าม session ถัดไปหยุด process ที่ไม่ใช่ของตนโดยไม่ถาม.
- ห้ามแก้ `pol-core`, `.env.local`, production data, real PSP mutation หรือ Organization API dirty work.

## Constraints

- อ่าน canonical `.ai/shared/*` ก่อนทำงาน.
- ห้าม commit/push โดยไม่มีคำสั่งผู้ใช้; ห้าม push เข้า `main` หรือ `develop` ตรง.
- ห้ามเพิ่ม dependency หรือเปลี่ยน lockfile.
- ห้ามทำ real logout, SSO callback, Create/Edit/Test Credential หรือ PSP mutation.
- ก่อนแก้ไฟล์ dirty อื่น ต้องแยก ownership จากงานนี้ก่อน.
- Browser evidence ที่แก้แล้วใช้ safe local fixture และ HTTPS dev runtime ตามคำสั่งผู้ใช้; อย่าเรียกเป็นหลักฐาน authenticated production backend.
- `scripts/spec-trace.sh` ข้าม bugfix spec ที่ไม่มี `requirements.md`; exit ปกติไม่ใช่หลักฐาน F/B trace ครบ.

## Tests Run

- `rtk proxy npm test` -> Node `31/31`, root Vitest `330/330`, `@pol/shared` `26/26`
- `rtk proxy npm run typecheck` -> root, `@pol/ui`, `@pol/shared` ผ่าน
- `rtk proxy npm run lint` -> root และ workspaces ผ่าน
- `rtk proxy npx eslint scripts/dev-tls-ca.cjs scripts/lib/workspace-process.mjs scripts/lib/workspace-verification.mjs scripts/smoke-workspace-routes.mjs scripts/psp-contract-server.mjs` -> ผ่าน
- `rtk proxy npm run build` -> Next `16.3.1`, generate `115/115` routes
- `rtk proxy npm audit --omit=dev --audit-level=high` -> ไม่พบ production vulnerability
- `rtk proxy npm run verify:workspaces` -> root Admin `114` routes, code scan `683` files, active-reference `757` files ผ่าน
- `rtk proxy node scripts/verify-smoke-signals.mjs` -> `SIGINT` `130`, `SIGTERM` `143`, port released
- `rtk proxy npm run smoke:routes` -> Admin `/` `307`, `/admin/user/list` `200`, `/register` `404`
- `git diff --check` -> ไม่มีผลลัพธ์
- `.ai/bin/check-secrets.sh` -> ไม่มีผลลัพธ์
- `scripts/spec-trace.sh bugfix-smoke-cleanup-hardening` -> ข้ามเพราะเป็น bugfix spec
- `scripts/spec-trace.sh bugfix-psp-rbac-contract` -> ข้ามเพราะเป็น bugfix spec
- `scripts/spec-trace.sh bugfix-admin-proxy-evidence` -> ข้ามเพราะเป็น bugfix spec
- `scripts/spec-trace.sh bugfix-font-self-hosting` -> ข้ามเพราะเป็น bugfix spec
- `scripts/spec-trace.sh bugfix-auth-evidence-and-docs` -> ข้ามเพราะเป็น bugfix spec

Browser evidence:

- PSP route denial fixture: `/control/psp/list` ไป `/error/403`, พบ code `403`, ไม่พบ PSP request, console errors/warnings `0/0`
- PSP API denial fixture: ได้ PSP list `403` แล้วไป `/error/403`; ไม่มี request เพิ่มหลัง 2 วินาที; dev `StrictMode` มี initial aborted request หนึ่งรายการ
- Auth failure fixture: `/logout` คงหน้าเดิมพร้อม alert/retry; Account drawer คงเปิดพร้อม alert/retry
- Auth success fixture: `POST /admin/auth/logout` ได้ `204` และ `/logout` ไป `/login`
- Font standalone runtime: local TTF resources `11`, remote Google font resources `0`; `document.fonts.load` ของ 7 family/weight หลักเป็น `true`
- Production unauthenticated runtime: `/` ไป `/dashboard`, protected `/control/psp/list` ได้ `/admin/me` `401` แล้วไป `/login`
- Browser measurement ที่บันทึก: `clientWidth=1200`, `innerWidth=1200`

## Known Issues

- `trackManagedProcessTree()` ใช้ `ps` และยังไม่มี independent Windows implementation/proof สำหรับ detached descendant; Windows ยัง fallback ไป direct child handling เดิม.
- Authenticated production browser round-trip ยังไม่ทำ เพราะต้องใช้ real session; logout session revoke/audit ใช้ existing backend source evidence และ isolated fixture ไม่ใช่ real mutation.
- PSP/proxy/auth browser evidence หลักใช้ safe fixture และ dev runtime; production authenticated evidence ยังเป็น gap.
- `font` output เป็น local `.ttf`; หาก requirement ภายหลังบังคับ `.woff2` ต้องทำ conversion เป็นงานแยกพร้อม license/provenance verification.
- Existing bugfix specs เก่าบางไฟล์ยังเป็น historical artifacts; current scope reconciliation อยู่ในไฟล์ที่ระบุด้านบน.
- PreCompact hook เคย reject `PreCompact` `hookSpecificOutput` ด้วย schema validation; state จึงถูกเขียน handoff เอง.
- Task gate ต้องมีข้อความ non-empty บรรทัดเดียวกับ `Evidence:` แม้ `TESTING_PROTOCOL.md` แสดง multiline block; ใส่ summary same-line ใน tasks ใหม่แล้ว.
- Reviewer subagent จบด้วย stream error ก่อนคืน verdict; ห้ามอ้างว่าได้รับ formal reviewer approval.

## Next Recommended Agent

`bug-investigator` หรือ `auditor` แบบจำกัดไฟล์ สำหรับตรวจ Windows process cleanup และ review ความปลอดภัยของ
process-group tracking ก่อนขยาย scope. จากนั้นใช้ builder เฉพาะเมื่อผู้ใช้อนุมัติ follow-up; งาน docs/evidence ที่
เสร็จแล้วไม่ควรทำซ้ำ.

## Next Steps

1. เริ่ม session ใหม่ด้วย `Read .claude/handoff-2026-08-28.md` แล้วอ่าน `.ai/shared/PROJECT_CONTEXT.md`,
   `.ai/shared/CODING_STANDARDS.md`, `.ai/shared/ARCHITECTURE.md`, `.ai/shared/TASK_PROTOCOL.md` และ
   `scripts/spec-state.sh` ของทั้ง 5 bugfix spec.
2. รัน `git status --short` และแยกงานนี้จาก dirty Organization API, PSP UI และ network artifacts ก่อนแก้ต่อ.
3. ตรวจว่าจะปิด Windows detached cleanup ด้วยการ amend spec หรือเพิ่ม implementation/test; ห้ามเดา platform
   behavior จาก macOS/Linux.
4. ตรวจ authenticated production evidence ที่ปลอดภัยได้หรือไม่; ถ้าต้องใช้ credential หรือ mutation ให้
   หยุดและรายงาน evidence gap แทนการยิงจริง.
5. หลัง follow-up ทุกชุด ให้รัน `npm test`, `npm run typecheck`, `npm run lint`, `npm run build`,
   `npm run verify:workspaces`, smoke checks, `npm audit`, `git diff --check` และ secret scan ใหม่.
6. ก่อน commit ต้องผ่าน code review จริง; commit/push/PR ทำเมื่อผู้ใช้สั่งเท่านั้น.
