import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "ดูสำนักงาน | POL",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
