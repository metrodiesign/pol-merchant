import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create a new user | Dashboard - Minimal UI",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
