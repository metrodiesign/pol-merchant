import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "ดูข้อมูลผู้ใช้งาน | POL",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
