import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Account general settings | Dashboard - Minimal UI",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
