name: pol-merchant

services:
  pol-merchant:
    image: ${POL_MERCHANT_IMAGE:-pol-merchant:local}
    build:
      context: .
    environment:
      NODE_ENV: production
      PORT: "3002"
      HOSTNAME: 0.0.0.0
    ports:
      - "3002:3002"
    restart: unless-stopped
